# pixl-webui
Lightweight local WebUI for Stable Diffusion–family image generation on Windows — Next.js UI over a FastAPI + diffusers backend. Model browser, samplers, batches, live preview, prompt snippets, and img2img/IP-Adapter reference images.
